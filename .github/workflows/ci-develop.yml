name: CI - Develop

on:
  push:
    branches:
      - develop

jobs:
  build-and-test:
    uses: ./.github/workflows/_build-and-test.yml
    with:
      solution_file: Maliev.SearchService.slnx
      extra_args: -p:SharedLibraryVersion=1.0.*-alpha*
    secrets:
      gitops_pat: ${{ secrets.GITOPS_PAT }}
